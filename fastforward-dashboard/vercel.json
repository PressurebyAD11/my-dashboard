{
  "version": 2,
  "builds": [
    {
      "src": "api/[...path].js",
      "use": "@vercel/node",
      "config": {
        "includedFiles": ["client/dist/**", "server/data/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/[...path].js" }
  ]
}